# --- OS / Editor junk ---
Thumbs.db
Desktop.ini
ehthumbs.db
$RECYCLE.BIN/
.DS_Store

# VS Code
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# JetBrains IDEs
.idea/
*.iml

# Node / tooling caches (if you later add build scripts)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/

# Python caches (in case of scripts)
__pycache__/
*.py[cod]
*.pyo
*.pytest_cache/

# Backup / temp files
*.tmp
*.temp
~$*
*.bak
*.orig
*.swp
*.swo

# Image editor temp / metadata
*.xcf-backup
*.psd
*.kra~
# Exported working files you don't want in final pack (uncomment if needed)
# *.xcf
# *.psd
# *.kra

# Compression artifacts
*.zip
*.7z
*.rar
# (If you want to version release archives, remove those above or store in /releases)

# Logs
logs/
*.log

# Ignore local test worlds or stash directories if placed here (custom)
worlds/
scratch/

# Git merge / rebase artifacts
*.rej
*.patch
*.orig

# Mac resource fork files
._*

# Optional: ignore high-res source textures, keep only optimized (uncomment if desired)
# sources_highres/

# Keep pack.mcmeta always tracked
!pack.mcmeta
